gradle.startParameter.setInitScripts(gradle.startParameter.getInitScripts().findAll { !it.name.contains('ijMapper') })

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}
